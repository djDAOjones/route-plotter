the EXAMPLE BACKGROUNDS should be example projects that load up a background and full animatin payload. I have supplied zips to allocate to these

add switch on right side bar for straight vs curved paths

text position isn't get saving in the zip - check the zip is exporting all needed info

when global scale is altered the text is way to effected (maybe it shouldn't be effected at all)

add shift key modifier to readme / help

include an "mask others" slider on background, to fade out parts of the image that are outside the area shape (current waypoint + text should remain visible, but not paths)